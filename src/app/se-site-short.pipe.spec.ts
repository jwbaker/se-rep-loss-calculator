import { SeSiteShortPipe } from './se-site-short.pipe';

describe('SeSiteShortPipe', () => {
  it('create an instance', () => {
    const pipe = new SeSiteShortPipe();
    expect(pipe).toBeTruthy();
  });
});
